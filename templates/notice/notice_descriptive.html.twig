{% extends 'base.html.twig' %}

{% block title %}
    Annonce {{ notice.brand }}
{% endblock %}

{% block body %}
    <div class="container my-3">
        <div class="row">
            <div class="col-6">
                {# <img src="{{ vich_uploader_asset(notice.noticeImages[0], 'file') }}" class="d-block w-30" alt="{{ notice.noticeImages[0].name }}">
                <div>
                    {% for image in notice.noticeImages %}
                        <div class="d-flex mt-2">
                            <img src="{{ vich_uploader_asset(image, 'file') }}" class="d-block w-100" alt="{{ image.name }}">
                        </div>
                    {% endfor %}
                </div> #}
            --------------------------------------
            {# <div id="carouselExample" class="carousel slide">
                <div class="carousel-inner">
                    <div class="carousel-item active">
                    <img src="..." class="d-block w-100" alt="100">
                    </div>
                    <div class="carousel-item">
                    <img src="..." class="d-block w-100" alt="100">
                    </div>
                    <div class="carousel-item">
                    <img src="..." class="d-block w-100" alt="100">
                    </div>
                </div>
                <button class="carousel-control-prev" type="button" data-bs-target="#carouselExample" data-bs-slide="prev">
                    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                    <span class="visually-hidden">Previous</span>
                </button>
                <button class="carousel-control-next" type="button" data-bs-target="#carouselExample" data-bs-slide="next">
                    <span class="carousel-control-next-icon" aria-hidden="true"></span>
                    <span class="visually-hidden">Next</span>
                </button>
            </div> #}
            --------------------------------------    
                {# <div id="myCarousel" class="carousel slide" data-ride="carousel">
                    <div class="carousel-inner">
                        {# {% for image in notice.noticeImages %}
                            <div class="carousel-item {% if loop.first %}active{% endif %}">
                                <img src="{{ vich_uploader_asset(image, 'file') }}" alt="{{ image.name }}">
                            </div>
                        {% endfor %} #}
             {#           {% for image in notice.noticeImages %}
                            <div class="item">
                                <div class="thumbnail">
                                <img src="{{ vich_uploader_asset(image, 'file') }}" >
                                </div>
                            </div>
                        {% endfor %} 
                    </div>
                    <a class="carousel-control-prev" href="#myCarousel" role="button" data-slide="prev">
                        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                        <span class="sr-only">Previous</span>
                    </a>
                    <a class="carousel-control-next" href="#myCarousel" role="button" data-slide="next">
                        <span class="carousel-control-next-icon" ariahidden="true"></span>
                        <span class="sr-only">Next</span>
                    </a>
                </div> #}
                {% include "partials/carousel.html.twig" %}
             ------------------------------------------------   
            <div>
                    <a href="{{ path('app_contact', {'source': 'notice'}) }}" class="bg-info rounded text-light p-2">Contact</a>
                </div>
            </div>
            <div class="col-md-5 text-center">
                <h2 class="fw-bolder">{{ notice.brand }}</h2>
                <h3>{{ notice.model }}</h3>
                <div class="d-flex align-items-end justify-content-around bg-primary my-4 py-2 text-light rounded">
                    <h4>{{ ((notice.price) / 100) | number_format(0, ',', ' ') }} €</h4>
                    <h4>{{ notice.kilometer }} Km</h4>
                    <h4>{{ notice.releaseDate|date("Y")  }}</h4>
                </div>
                <pre class="badge bg-light rounded">
                    {{ notice.description|raw|replace({'&nbsp;': '\n'})|striptags|nl2br }}
                </pre>
            </div>
        </div>
    </div>
{% endblock %}